# check_voice.py baseline -- build vocabulary in notes the player reads
# 0 known problem(s), recorded so the gate can fail on NEW ones.
# Shrinking this file is the point. Regenerate deliberately with
# --write-baseline; nothing rewrites it on failure.
